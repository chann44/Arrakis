version: "2"
sql:
  - engine: "postgresql"
    schema: "sql/schema.sql"
    queries: "sql/queries"
    gen:
      go:
        package: "db"
        out: "internals/db"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_json_tags: true
